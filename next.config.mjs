/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [ "i.pinimg.com","encrypted-tbn3.gstatic.com"], // Add domains you want to allow images from
      },
    //  "cdn.dribbble.com"
};

export default nextConfig;
